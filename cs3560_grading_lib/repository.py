from pathlib import Path
from typing import Optional

from git import Repo


def repo_from_archive_file(path: Path | str) -> Repo:
    """
    Extract the archive file and initialize Repo to it.
    """
    pass
